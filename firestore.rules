rules_version = '2';
service cloud.firestore {
match /databases/{database}/documents {
// Collection to store user-specific growth history
match /userGrowthHistory/{userId} {
// Allow read/write only if the request's user ID matches the document ID
allow read, write: if request.auth.uid == userId;
}
}
}